# Polybot TODO

## NOW 

- [ ] **DEPLOY-GATE（明天第一件）— review trades-cap 对 11 lens rep 的影响, 再 deploy** — 今日 11 rep 已入 `paper_candidates`(`lens_jaccard_20260614`)但**未上 ACTIVE/未 deploy**. 明天: ① 对每个 **trades-based** rep 算 `capped_frac`(触发 candle 撞 4k cap 比例)→ 只 deploy `runtime_ok=1 ∧ 低 capped_frac`(2 个 `bn_` klines 零污染最稳; 6 个 pmt_/intra 需逐个看 capped_frac); ② deploy 一并切换: ACTIVE 本地已删 4 个死 factor + 待加可信 rep(live=False). 看板 `SELECT runtime_ok,net_base_wilson,nev,json_extract(diag,'$.ep') ep,json_extract(diag,'$.coverage') cov FROM paper_candidates WHERE cycle_tag='lens_jaccard_20260614' ORDER BY net_base_wilson DESC`.
- [ ] **⚠️ DATA-QUALITY — PM trades 4k-cap 污染 bt（2026-06-14 重大发现, 影响所有 trades mining）** — PM data-api `/trades` 服务器硬限 **offset≤3000 → max 4000 trades/cid, newest-first → >4k 笔 candle 丢最早 trades**. ∴ bt(data-api capped)≠ scanner(WS 完整)→ **trades-based INTRA 特征在高量 candle base 值发散**(实测 full-day 仍 17% candle 不一致, batch min 系统偏高 = 漏早期低价单). min_120_dn = poster child: net_base+0.75/effN147/drop5+0.9 **全好看但无关**(测的是"bt 样本真不真", 非"runtime 能不能复现")→ bt↔paper 触发集 Jaccard **0.15**(85% 不重合), +109% bt edge 在 bot 不交易的 candle 上 = 不可达. **cap 禁区刻画(非全废)**: 危险 = **早窗极值(min/std/chg_rate over [cs,120/180]) × extreme rolling 阈值(rank>0.97/zs>1.6, 参考分布也污染) × 触发集中放量 candle**(三占=min_120_dn); 安全 = **晚 entry(et≥180 cap 保近端)/ plain 阈值 / 不挑量 / <4k candle**. **klines(`bn_`)=零污染脉**(=R4 存活之因). 出路: ①mining 加 `capped_frac` gate(>~0.2 降权) ②前向录 WS trades 建干净历史 ③倾斜 klines/跨场(GROW②). 行业普遍(survivorship + backtest-live divergence; crypto tick 最脏 klines 最净). 证据: scratch/{review_min120dn_bt, review_min120dn_btvspaper, probe_transform_drift}.py.
- [ ] **NS2 — R4 decay monitor（现=0, 真金裸奔, 最高优先）** — gates.py 仅 docstring 无实现; paper_active_agg=累计/滞后看不到 decay. 机制驱动设计(见 P5): `edge = wr − ep − fee`, **盯两个 trailing 趋势(滚动均值, 非单点)**: ① **ep 上漂** = 套利者补 PM 定价漏洞(机制 ii); ② **wr 下滑** = BTC 反转变弱(机制 i). **撤点 ep≈0.58**(net EV 跌破 5% hurdle; **非 0.65 — 7% fee 把窗口压窄**). **⚠️ 震荡≠decay, 别杀错**: **wr 掉·ep 稳 = 震荡**(暂时坏 regime 会回, 如 4 月)→ **HOLD**(靠 5% sizing 扛, 见 ramp 纪律); **ep 结构上漂 = decay**(套利永久补洞)→ **demote**. ∴ demote 触发 = ep-creep, 或 wr 跌破历史震荡谷底且持续, **不是单月弱**(否则=失败的 regime-timing + churn cost). 挂 R4 paper-shadow(n=147>live13 更快). 滞后兜底=drawdown-from-peak ratchet(封顶 giveback). ⚠️ **统计 regime sensor 已死**(regime_probe: vol regime 仅 ~10min 非 R4 driver; session 在 paper OOS **反号=p-hack**; vol/trend/funding/OI 全弱; R4 全时段普遍赢)→ 领先信号只能**机制派生(ep)**, 非统计分桶. **领先层最终裁决(用户洞察)**: 任何能预报 R4 弱周的信号 = 一个 mining 条件 → 折进入场(走 feature+重挖, 见 GROW③), **不存在独立"监控器"**; 预报不了 → 只剩 PnL. ∴ **NS2(R4)=wr-trend + ep-trend 滞后监控, 无领先层**(R4 边主盯 **wr**: ep 稳定 ~0.55, 震荡来自 wr/反转强度). 证据: scratch/research/{regime_probe,paper_session_validate,r4_emergence}.py.
- [ ] **NS1 — mine refactor（IN PROGRESS, `scratch/research/mine/`, uncommitted）** — compute/selection 分离
  **✅ select 完成 (2026-06-14)**: 在 **clean features**(pm/basis 已删, 1087 列)重挖 → 17 fit-for-size → **11 dedup-rep** 落 `paper_candidates`(cycle_tag `lens_jaccard_20260614`). **schema 重定 = gate 实列 + `diag` JSON**(弃原 ALTER-多实列, churn 免疫, 见 CLAUDE.md ADHD-SSOT.4): 实列 net_base_wilson/nev/n_hit/wf_sign_frac/lens_count + runtime_ok; 易变诊断(wr/ep/effN/eff_frac/coverage/n_variants/legacy v1v2)入 diag. 保 audit-SSOT 单表(verify_active_audit 强制 ACTIVE⊆paper_candidates.expr). 4 个老 per_dollar factor paper-OOS 转负 → killed(factor_decisions); ACTIVE = R4(live)+std_180_up. **未 deploy** → 见 NOW 顶 DEPLOY-GATE + DATA-QUALITY(11 个里 2 futures runtime_ok=0, 6 trades-based 受 4k-cap 嫌疑待 capped_frac 复核). **dedup = Jaccard trigger-overlap + greedy**(见下 ⑤). **入选宽进**(kill-unless-proven; 禁用 in-sample 指标 pre-filter=priors-override-data); paper 须 **per-factor 独立记账·无 FCFS 锁**(待确认 scanner). ② **诊断三件套 (三轴正交)**: Wilson 收缩(治 wr **小 n 不可靠**)=net_base_wilson **gate+rank** (z=1.0, `WILSON_Z`); **effN(治 少数赢单垄断)已落地** — compute emit `effN_up/dn`=`(Σ_wins pay)²/Σ_wins pay²` 参与比 ∈[1,n_win](纯 Σ, 同 nev 等级的 fact), lens 折向 `effN`+`eff_frac`=effN/n_win, **诊断列非 gate**; **drop5 待做**(order-stat 要逐笔, Σ 反推不出 → verify 阶段对 survivor 重建 mask、删最肥 5 笔重算 nev, 转负=运气). convex **已删**: `nev−net_base` 不是纯凸性(base 用全样本 ep、nev 只在赢单取 ep, 混进 win-ep 选择项 → 多数为负), effN 取代. **最严谨=用 effN 而非 n 做 Wilson = risk-adj t**. ⚠️ 三者皆 in-sample 预审降权, **非 proof (FWE), 真 proof=paper-t**. ③ **longshot live 两关**: ①过 mirage 关(②证非凸性假象) + ②过 variance 关(N 个真独立 factor 分散 σ/√N + 极小 Kelly 权重 + maker stop-loss 截尾) → **longshot = 组合成熟后边际增量, 非单独上线**. ④ **选集 = overlap 梯度, 非离散簇** (无 plateau → 无数据驱动正确 T; Jaccard T0.65→40 / **0.75→52**(宽进, 用) / 0.85→64). dedup = **低风险 dial**; 靠 paper-t + portfolio 矩阵精淘. 活在 `scratch/data/{lens_candidates.parquet, survivors_review.csv}`(现 **Jaccard-52**, verify_dedup.py 已完善). 证据: `scratch/research/mine/{compute,lenses,verify_dedup,show_wilson}.py`.
  ⑤ **dedup SSOT (重定: PnL-corr 退役 → 定 Jaccard trigger-overlap + greedy)**: 度量 = **Jaccard 触发集重合 (|A∩B|/|A∪B|), 非 PnL-corr 非 containment**. **PnL-corr 两变体实测皆废**: union-corr 负数全伪 (Pearson 减均值 → disjoint 得 −μ_Aμ_B<0; 几何正交(点积=0)≠统计无关(corr=0)), 但**对聚类无害** (sub-floor pair 早被 co-active≥30 floor 清零, reliable 无负); intersection-corr (只在共成交 candle 算) **退化**: 条件在"俩都开火"后同向 factor 共享 candle 结果 `won` → corr≈1 机械必然 → 双峰 (0 或 ≥0.9, 中间空) → **T 失效 (0.5~0.9 恒 9 簇)** + 传递性过折 (142→9). 根因: PnL 缠 ①触发位置(要)+②盈亏值(不要, won/ep 主导); intersection 把①条件掉只剩退化②. **∴ dedup 是 set 问题(触发集), 非 return 问题(收益共动) → Jaccard**. **Jaccard 非 containment**: Jaccard 对称 = 双向等价 (A⟺B = 同假设); containment(|∩|/min) 不对称 = 单向蕴含(B⊂A 特例) → **罕见窄因子被任意覆盖它的宽因子误吞** (丢 distinct sub-hypothesis). 今日数据两者几乎不分歧 (同向 pair size ratio median 1.0, 无极端嵌套) → 此选 = **原则/鲁棒性** (longshot/ETH 扩张会造极端嵌套) + **错向宽进-safe** (误折=永久丢假设·不可逆; 误留=温和浪费·paper-t 自杀). **algo = greedy leader-selection**: 按 net_base_wilson 降序, 候选只撞**已保留代表集 S** (非全 pairwise → 避 chaining), 同向 ∧ Jaccard>FOLD_J 则折否则新建; S 种子 = [R4]; mask **∩ entry_valid** (p_intra 非空 = 真可成交 candle). **三操作别混 (旧 ⑤'一张矩阵跨阶段'是陷阱)**: SELECTION(现, greedy+trigger-overlap → 子集) ≠ CORRECTION(effN→paper-t 严格度, 一个数, later) ≠ PORTFOLIO sizing(return-cov+Kelly, NS3); 前者对象=trigger-set, 后两者=return-cov, **不同对象**, 不复用同一矩阵. **状态**: verify_dedup.py 已完善 (Jaccard+dirseg+entry_valid, FOLD_J=0.75 → **52 rep**, survivors_review.csv 待逐行 review); select.py 待写 (seed S=[R4], 写 paper_candidates, ALTER lens 列); gates.py `FACTOR_DEDUP_OVERLAP_MAX=0.45`(containment) 退役 → Jaccard `FOLD_J=0.75` (待 commit+audit). 证据: verify_dedup.py(=定版; PnL-corr 脚本已删, 结论见上 prose).
  ⑥ **wf 不 recency-weight** (今日定): recency = decay-timing = NS2 的活 (selection 要的是跨 regime 一致性=等权)·16 周付不起 down-weight·烤进 peak-chase 偏差; wf>0.9 硬卡 = 伪精度 (4-14 周, 0.8 vs 0.9 差一周噪声) + **倒置证据** (4/4@4wk 强过 11/14@14wk?). 真要升级 = **week-level Wilson (n-aware, 同 wr 的修法升一层), 绝不 recency**; 现维持等权粗筛. ⚠️ wf 小 n 与 coverage 连动: oi/ls 仅 4 周 = futures 晚 ingest (April-only, 见①coverage 标记).
- [ ] **EXIT — maker-exit 可行性（先查 PM maker fee, docs-first）** — 现全策略 hold-to-settlement(结算免费, 只付入场 7% taker). early-exit 走 taker=round-trip 14% 不划算; 走 **maker(挂单)** 若费低 → 解锁 **① 方差压缩→更大 Kelly + ② stop-loss→砍 MDD + ③ 多 factor 时序前置(见 NS3 解法 Z: 无便宜平仓→combiner 要么牺牲早入场要么做不了动态净额)**, 近白嫖. sell≠当 MM(只是被动平仓); MM=终局, 卡 capital+infra. 动作: llms.txt → fee schedule 确认 maker 费. **启用判据 (今日2 定): cost/benefit 满意才用 — 否则 over-design; 现阶段 FCFS 足够 (见 NS3), maker-exit = 条件触发的可选项, 非路线必经下一步; 不为「还不存在的多 factor 问题 + 成本收益未满意的平仓机制」提前做工.**
- [ ] **NS3 — portfolio 聚合层 (layer 3, multi-factor 硬前提; 未触发, 仅记地形)** — **触发门槛 = ≥2 个真独立 live factor**; 当前 R4 single 无冲突, **现在 0 动作**, 只提前看清地形. 职责在 **portfolio 层(非 factor/strategy 层)**: ① **conflict netting** — 同 event 反向 factor 在 **edge/概率层**融合(非 size 层投票): gross-edge 加权 → 净 edge 跨 fee 门槛才下单, 否则弃(反向相减常把净 edge 压到 fee 以下→弃单; **冲突 = 低置信信息, 自动缩仓**, 非异常). ② **overbet 防护** — 同向 factor **不能 size×N**(相关则 edge 重叠, 简单加 = 超真实 Kelly = 破产), 需相关折扣 + 联合 Kelly. **前提 = dedup 保独立性**(edge 线性聚合才成立). **⚠️ corr-for-sizing 现 FCFS 下不 bind** (同时仅 1 仓 → overbet 物理不可能; 只在 GROW 多市场 / post-FCFS netting 触发; 用的 corr 矩阵 = **return-cov 的 OOS 估计**, 与 bt-dedup 的 trigger-overlap **不同对象** (dedup 测触发集重合/FWE, portfolio 测收益共动/Kelly; 别混, 见 NS1 ⑤). **命名 SSOT**: feature→factor(=现 strategy, 单规则)→**portfolio(新层, 聚合)**→execution; **别把聚合层叫 strategy**.
  **核心难点 — entry_time × combiner 时序张力**: 每 factor 独立 entry_time + hold-to-settlement 与**无成本 combiner 不兼容**(时间单向, t=60 下单看不到 t=180 才出现的信号, 不可回溯净额; 朴素时序执行 = up+dn 双下 = 双 fee + 对冲 = 最坏). 两解付不同代价: **Y = 统一 entry cutoff**(牺牲早入场, **需重 mine**: entry 固定=cutoff, combiner 单时刻干净, hold-to-settlement 保留) / **Z = 保留 entry + maker-exit 增量净额**(牺牲零平仓成本, 保早入场 alpha, **依赖 EXIT 项**). **决策先实测「早入场 alpha 值多少」**(entry=60 vs 240 edge gap; 5m 窗短→直觉代价小但要测; 晚 cutoff ep 更极端→赔率结构变, 整体重测). 机构对照: portfolio construction = 成熟学科(Markowitz 1952 诺奖→Black-Litterman→risk parity), 我方 EV-netting+Kelly+overlap-dedup = 其 **PM-binary + 7%fee + 小 capital 简化版**(机构股票多空 momentum vs reversal 冲突 = 同款 netting; 机构在股/期/期权/FX 不来 PM = 容量/fee/监管 = 我方护城河).
  **MVP 决策 (今日2, 按体量定): 现阶段 combiner = FCFS (先到先得, et 最小先触发 → 锁死整个 candle, 后续不许 trigger)**, 之后再优化. 用约束消 combiner 全部复杂度(无 netting / 无时序张力 Y/Z), 匹配 small-capital / few-factor. **实测支撑** (19 rep, 全史 30687 candle): 共触发 27.6%(占活跃 candle), 结构 = 76% 同向叠加(代价=under-bet 少赚, 温和) / 24% 反向冲突(代价=满仓下早单、无视反向, **真风险面仅 6.6% candle**); 逆向选择仅 11%(time≠edge 但 even-band net_base 集中 → 挤掉更强者罕见). **FCFS cap ballpark ~< 20-30 live factor**(共触发随 factor 数增长, 超过才升级等权 netting); 现 1 个 live, 离 cap 极远. **唯一真风险 = 反向冲突满仓 → 上线监控这 6.6% candle 的 PnL**. ⚠️ **untested narrative 警示**: 「叠加扩 profit / 冲突降 loss」是**理论假设, 未在 PM 5m 验证**; bt 测不可信(in-sample selection-bias 幻觉, 这些 candle 本就是 mining 挑的); **只能 paper-OOS 多 factor 验证 — 上 combiner 前必须实测, 严禁凭理论上复杂度**. 演进: FCFS → 等权/逆方差 netting(factor 增多) → 强正则 MVO/BL 精神(远期样本足); **方法由样本量定, 非先进度**(bias-variance: 样本少→简单端胜; DeMiguel 1/N 打败优化 / Michaud MVO=error-max).
- [ ] **GROW — 横向扩张 + 新 feature 家族（贴 edge, 非换市场; R4 验证跨场矿脉真 → prior 加更多输入, 但"矿脉真"≠"每 feature 都 signal", 各自仍过 mining OOS）** — ① **ETH/SOL 5m**(同币安→PM 结构, proven edge type, 同基建, 最低风险) ② 更多 **跨场订单流信号**(funding/liquidations/OI/options flow + 更多 taker/vol 横向)挖进 crypto 5m ③ **新 feature 家族「多日反转 regime」**(return autocorr / variance-ratio over 1d/7d, **leak-free 全用入场前数据**) — 动机=R4 edge **周级震荡**(Mar 强/Apr 弱/May 强, 随 BTC 反转强度变, 见 r4_emergence.py); 实测**现特征集最长仅 1h、0 个反转/regime 度量**(`__zs7d` 只是归一化器, 非自相关). **补在 feature 层(加输入)+重挖+NS1-b weak-parent, 非 expr 层手搓(=解密/p-hack)**. **不做 politics**(无结构优势/换认识论·基建/丢护城河). ("BTC 5m 榨干"是错觉: 方法坏了(longshot 偏置)+只挖了贫矿脉; 1 factor·5% sizing 离容量饱和极远.)

- [ ] **P1 — 还 1 项 infra 债（抬未来速度, 可选）** — 候选: deploy-free "停 live 保 paper" 开关 / 扩 test (settle+redeem 路径未覆盖) / fractional-Kelly 准备. (transforms 已 spec-SSOT 稳, 移出; 见 NEXT-1.)
- [ ] **P5 — R4 机制（假设已成型, 给 decay-prior + conviction, 不抽可交易条件=那是 mining/p-hack）** — 假设: R4 = **fade 放量激进买入高潮(exhaustion 反转) + 跨场信息领先**(币安=价格发现, PM=滞后 wrapper). edge 拆 = 真 BTC 反转(机制 i) + PM 低估(机制 ii). 微观结构文献先验(短周期 order-flow reversal, Lehmann 1990). **emergence 实测(r4_emergence.py)**: ⚠️ **V2 cutover(4/28) 不是 cause**(edge 最强在 3 月 V1, 最弱在 4 月 cutover 附近); edge **按周震荡**, 变量是 **wr/反转强度(机制 i)**, 而 **ep/PM 定价(机制 ii)稳定 ~0.55** → R4 跟 **BTC 反转 regime** 走, **非永久 edge(4 月近零一整月)**, 且 R4 是 5 月好 streak 上 graduate(paper 没见过 4 月弱期). decay 路径: 短期=wr 掉(反转 regime 转趋势), 长期=竞争(MM bot→ep 上漂). 币安长史反转测=已决定不做(t>2.5 已证 signal).

**🔭 观察中 (DEPLOYED, 真金 live)**: R4 armed @ 5% ($36→$1.80/笔), **live 执行已验证 (P1 通过, drift +0.14¢)**, live fills 流动中. dry spell 历史上限 29h. 硬急停 = `ssh vps systemctl stop polybot`. 看板: `SELECT * FROM paper_active_agg` (polybot_live.db); 候选 `SELECT * FROM paper_candidates WHERE cycle_tag='lens_jaccard_20260614'` (pm_btc5m.db). **ACTIVE = R4(live)+std_180_up**(4 个老 per_dollar paper factor 6-14 killed, 本地待 deploy).

- **sizing**: `BANKROLL_FRAC` = 押本金 (bankroll) 固定 % (不看 edge), **NOT Kelly** — 改名消 KELLY_FRAC 歧义 (旧名骗人, 代码做 fixed-fraction). R4 full-Kelly≈26%, **5%≈0.2× = 保守** (应对 marginal CI + 首次真金 + live 代码没跑过). fractional-Kelly (λ·f\*, λ=0.25–0.5) = 未来升级, 待真金 OOS 收窄 edge 估计.
- **config-as-code**: 逻辑+arming+sizing 留 code (git 审计 + deploy test 闸门), **不 refactor 成 DB-driven** (会丢审计+闸门, 对动钱旋钮是降级非升级). 真机构: logic→code+review+CI, 仅急停/高频操作→可轮询 store. 硬急停 = `systemctl stop` (已 deploy-free). 细粒度"停 live 保 paper" = 可选缓 (见 INFRASTRUCTURE).
- **testing 策略**: test pyramid — unit(纯数学) / integration(Scanner+tmp db+录制 fixture, =smoke test) / contract(只读真 API + live 下单 dry-run). 铁律: ①测试不可碰资金 (LIVE=False + 真 key guard); ②fixture=录制真 response; ③main.py 跑=部署后观察, 非正确性闸门. contract/live-dry = N6 (arm R4 前置).
- **migration 策略**: 单 VPS 单进程可短暂停服 → stop-the-world (停服迁移), 不用 expand/contract (零停机, 多实例才需). 必须: versioned 脚本 + 迁移前备份 + 拿 prod 副本 dry-run.
- **R4 graduate 数据 (synced 2026-06-02)**: n=131 settled, winrate 64.9%, net EV **+20.7%** (std 94%), **t=2.45** (过 gate), 95% CI **[+4.1%, +37.2%]** (下界擦 5% hurdle), MDD -$9.27, last40 +30.8% (近端更强无 decay). → GRADUATE 成立; 5% 小额上线应对 marginal CI.

- **factor 准入诊断 (base/$1 + tail 风险, 2026-06-06 review)**: nev = 真实 EV (SSOT, `mean(wonᵢ/epᵢ−1)` 逐笔) 但 **tail-sensitive** — 几笔低 ep 赢单的 `1/ep` 凸性能虚抬它. 拆解给诊断:
  - `margin = wr − ep` (per-share base); `base/$1 = wr/ep − 1` = **tail-agnostic 下界** (无凸性的 nev); `净base = base/$1 − 7%fee`.
  - **净base > 0 = edge 有不靠运气的底座**; 净base ≈ 0 → nev 全靠凸性 (OOS 未证, 仅 paper-t 能验).
  - 集中度: `effN = (Σpayoff)²/Σpayoff²` (几笔赢单在撑, payoff=1/ep−1); `drop5` (删 5 笔最便宜赢单后重算 nev, **转负 = edge 是运气**).
  - **定位 (关键, 别越界)**: nev 仍 **primary**; base/$1·effN 是**诊断 + 软排序**, **NOT 硬 gate** (会误杀真·散凸性 edge), **NOT 取代 nev**. gate 仍是 nev(量级) + paper-t(置信, 自动惩罚 tail). tail metric = 给人看的显微镜, 解释"为何 t 低".
  - **我方约束裁决**: 7%fee + 小资本 + 单 factor 无法分散 tail 方差 → 现阶段只期望 **净base>0** 的 factor graduate; tail factor (净base≈0, edge 全凸性) 结构性不利, **待 10+ 独立 factor 能分散方差再评估**. moat 视角: 7%fee = 城墙, 经济性排除薄-edge 套利者, 留零售 fish + 我方大 edge.
  - **实测 (V2 bt)**: MIN120DN 净base **+46%** (真底座, 稳); CHG×2 净base ≈0 (薄, edge 全凸性, 散凸性故非 mirage); MIN180UP/STD180UP effN 9–11 + drop5 转负 = **tail mirage** (MIN180UP paper 已印证 t=−4.12).
- 看板: sync 后 `SELECT * FROM paper_active_agg` (polybot_live.db) = ACTIVE factor 实盘.

## ML PLAN B (如重挖 0 cross-bucket, 启动) — 本轮 199 survivors, 未触发
- LightGBM 找 non-linear interaction → feature importance → 翻译回 rule (`ml_methods.md` §5). 100K+ events 才考虑 MLP/Transformer.

## INFRASTRUCTURE
- **trades async httpx + Clash proxy wedge** — orchestrator 已 `timeout` 兜底 + trades 排末位; 根治需 subprocess curl (backfill v3).
- **LIVE_ENABLED=True + R4.live=True = 真金 live (2026-06-02 起)** — 仅 R4 动钱 (5% bankroll), 5 paper factor 仍 live=False. 本地跑 main.py 会读 PRIVATE_KEY → 必须 LIVE=False (no-fund-touch 铁律).
- **(可选, 缓) deploy-free "停 live 保 paper" 轮询开关** — scanner 每 loop 读 flag (文件/env/runtime_config 表), 只能停 live 不能 arm. systemctl stop 已兜底硬急停, 故非紧急.
- **(未来) fractional-Kelly sizing + ramp 纪律** — 待真金 OOS 收窄 edge, BANKROLL_FRAC 升级为 λ·f\* (按 w/ep 算 per-strategy Kelly, λ=0.25–0.5). **ramp 纪律(应对 gate-graduate-near-peak 偏差)**: paper→live gate 的 trailing t/EV 在好 regime 最高 → **系统性在 regime 顶部附近放行 → 上线即骑震荡下行**. 故 **R4 保持 5%, 直到 live 里熬过一个完整震荡周期(一个坏 regime + 复原)才 ramp**; size up 用 "survived ≥1 oscillation cycle" 换, **不用 "一段好 streak" 换**. 震荡期 = HOLD 不 demote(见 NS2 震荡≠decay).

## OPEN QUESTIONS
- Q2. Transform 历史查询跨大 gap 拉 stale data — 加 cs ≥ now-N*300 时间窗?
