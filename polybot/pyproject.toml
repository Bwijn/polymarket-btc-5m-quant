[project]
name = "polybot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx",
    "websockets",
    "sqlmodel",
    "py-clob-client",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
