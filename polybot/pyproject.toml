[project]
name = "polybot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx",
    "sqlmodel",
    "pandas",
    "websockets",       # PM CLOB market channel (book / price_change push)
    # py-clob-client* deferred until live phase (paper does not place orders).
    # When promoting to live, switch to py-clob-client-v2 (V1 cutover 2026-04-28).
]

[tool.pytest.ini_options]
testpaths = ["tests"]
